.hdsearch-secrets.json
.run/
# env files carry secrets — never commit. Convention: .env (dev) + .env.prod (prod);
# .env.example templates ARE committed.
.env
.env.prod
.env.selfhost
.env.local
.env*.local
api/.env
api/.env.prod
web/.env
web/.env.prod

# OS / editor / local tooling
.DS_Store
.claude/
